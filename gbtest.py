# Everything is same just replace the decision tree model from this model

GradientBoostingClassifier(subsample=0.8, min_samples_split=500, min_samples_leaf=50, max_depth=8, max_features='sqrt', random_state=10)
